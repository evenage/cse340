<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <%# messages() %>
            <% if (errors) { %>
                <ul class="notice">
                    <% errors.forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>,
                <% }%>

                    <h1>Delete Confirmation</h1>
                    <p>Confirm Deletion - The delete is permanent.</p>
                    <form action="/inv/delete" method="post">
                        <fieldset>

                            <label for="invMake">Make</label>
                            <input type="text" readonly <% if(locals.inv_make) { %> value="<%= locals.inv_make %>" <% }
                                    %>>


                                    <label for="invModel">Model</label>
                                    <input type="text" readonly <% if(locals.inv_model) { %> value="<%= locals.inv_model
                                        %>" <% } %>>

                                            <label for="invPrice">Price</label>
                                            <input type="text" readonly <% if(locals.inv_price) { %> value="<%=
                                                locals.inv_price %>" <% } %>>


                                                    <label for="invYear">Year</label>
                                                    <input type="text" <% if(locals.inv_year) { %> value="<%=
                                                        locals.inv_year %>" <% } %>>




                                                            <input type="hidden" name="inv_id" <% if(locals.inv_id) {%>
                                                            value="
                                                            <%= locals.inv_id %>" <% } %>>




                                                                    <button type="submit">Delete
                                                                    </button>
                        </fieldset>
                    </form>