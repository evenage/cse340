<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <%# messages() %>
            <% if (errors) { %>
                <ul class="notice">
                    <% errors.forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>
                <% }%>
                    <li><a href="/account/account-management">Welcome Basic</a></li>
                    <li><a href="/account/account-management">You're logged in</a></li>
                    </ul>

                    <main>

                        <% if(accountData.account_type !="Client" ){ %>
                            <h3>Inventory Management</h3>
                            <p><a href="/inv/">Manage Inventory</a></p>
                            <% } %>
                                <p><a href="/account/update/<%= accountData.account_id %>">Edit Account Information</a>
                                </p>
                                <p><a href="/account/logout" class="btn btn-danger">Logout</a></li>
                                </p>

                    </main>